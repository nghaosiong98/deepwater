import React from 'react';
import { PageHeader } from 'antd';
import './header.css';

const Header = () => (
  <PageHeader
    className="page-header"
    title="title"
  />
);

export default Header;
